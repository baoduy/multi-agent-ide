/**
 * Re-export from the shared package for backward compatibility.
 * The canonical implementation lives in @magenta/shared/sanitize.
 */
export { sanitizeWorktreeName as sanitizeName } from "@magenta/shared/sanitize";
