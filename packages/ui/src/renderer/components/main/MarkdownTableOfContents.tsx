/**
 * DEPRECATED — this file is orphaned and can be deleted.
 *
 * The inline Table of Contents that previously rendered inside
 * `FileViewer` has moved to a dedicated right-sidebar panel. See
 * `MarkdownTocPanel.tsx` for the replacement, and
 * `store/markdownPreviewStore.ts` for the state plumbing that connects
 * it to the active FileViewer.
 *
 * Nothing imports this module. Remove the file whenever convenient.
 */
export {};
